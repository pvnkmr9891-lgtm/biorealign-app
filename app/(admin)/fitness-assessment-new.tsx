// Thin admin wrapper around the coach's fitness-assessment-new screen —
// same component, just registered under (admin) so admin can launch the
// entry flow directly from ClientProfileView's Fitness tab, mirroring how
// client-profile.tsx wraps ClientProfileView for other features.
export { default } from '../(coach)/fitness-assessment-new';
